---
// Redirect old /game?level=X URLs to path-based /level/X
const levelParam = Astro.url.searchParams.get('level');
if (levelParam && /^[1-6]$/.test(levelParam)) {
  return Astro.redirect(`/level/${levelParam}`);
}
return Astro.redirect('/');
---
